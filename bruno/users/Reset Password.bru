meta {
  name: Reset Password
  type: http
  seq: 6
}

post {
  url: {{BaseUrl}}/api/v1/users/reset_password/
  body: json
  auth: none
}

body:json {
  {
    "email": "JohnDoe@example.com"
  }
}

body:multipart-form {
  email: JohnDoe@example.com
  password: hahaha123
}

script:post-response {
  let data = res.body;
  bru.setEnvVar("access", data.access);
  bru.setEnvVar("refresh", data.refresh);
  
}
